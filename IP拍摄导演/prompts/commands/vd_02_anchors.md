# /vd_02_anchors - 证据锚包（让话不飘）

根据 **项目根/outputs/inputs/brief.md** 生成 **项目根/outputs/{时间戳}-{主题概括}/02_anchor_pack.md**。运行目录 = 用户指定 或 **项目根/outputs/** 下最近包含 **00_plan.md** 的子目录；若无则按 brief 解析并创建（同 vd_01_topic）。

## 必须生成

- **电影锚 3 个**：每个写「画面描述 + 可引出的观点 + 可做字幕的一句」
- **现实锚 1-2 个**：没有就提示「需补充」
- **强类比 1 个**（可视化、可转述、与主题同构）
- **边界条件 2 条**（防翻车）

并做 **6 发动机自检**（勾选+一句理由）。

## 文件契约

- 输出文件顶部必须写：目标 / 输入 / 输出 / 6 发动机自检
- 只写入 **项目根/outputs/{时间戳}-{主题概括}/02_anchor_pack.md**

## 参考

遵守 director_skill_v1.mdc。证据锚用于现场提问时「把话拉回地面」，避免飘。
